<div class='page'>
  <div class='player-cards'>
    <div class='new-cards'>
      <% settings.player.show_hand.each do |card| %>
          <div class='card <%="#{card[0]}-of-#{card[1]}" %>' ></div>
      <% end %>
    </div>
  </div>
  <a class='btn' href='/hit'>Hit</a>
  <a class='btn' href='/clear'>Clear</a>
</div>

<%= settings.player.show_hand %>
